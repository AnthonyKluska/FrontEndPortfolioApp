import React from 'react';
import { Button } from 'reactstrap';

const Example = (props) => {
  return (
    <div>
      <Button color="primary">primary</Button>{' test '}
      <Button color="secondary">secondary</Button>{' test '}
    </div>
  );
}

export default Example;